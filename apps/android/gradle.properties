org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Safe runtime defaults live in app/build.gradle.kts. Put public flavor-specific
# values only in ignored networkpeer.development.local.properties and
# networkpeer.production.local.properties. See README.md.
